# Documents Folder

Place PDFs here when you want to ingest them from the terminal:

```bash
python ingest.py
```

The Streamlit app can also upload PDFs into `uploaded_docs/` and ingest both folders.
